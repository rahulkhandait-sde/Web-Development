// Create a new array using map function whose each element is equal to the original element plus 5

let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];

const addFive = arr.map((num) => num + 5);
console.log(addFive);